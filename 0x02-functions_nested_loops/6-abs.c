#include "main.h"

/**
 * _abs- compute the value of an integer
 *
 * @n: the int the questions
 *
 * Return: the abs of the int.
 */

int _abs(int n)

{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
